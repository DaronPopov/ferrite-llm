[package]
name = "hello-inference"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ferrite-sdk = { path = "../../../crates/ferrite-sdk" }
wit-bindgen = "0.37"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
