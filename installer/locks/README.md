# Locks

Generated lockfiles belong here.

The intended contract is:

- manifests declare desired graph
- profiles select providers and feature bundles
- lockfiles record resolved commits, URLs, checksums, and selected variants
